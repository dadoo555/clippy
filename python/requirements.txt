# Gemini SDK (unified google-genai) — includes the Live API. Import: `from google import genai`.
google-genai>=1.20
PyYAML>=6.0,<7
# Loads the API key from a .env file (dev and prod), so it need not be set by hand in the shell.
python-dotenv>=1.0,<2
# Microphone capture + speaker playback for the Live API (PortAudio wrapper).
sounddevice>=0.4,<1
# Offline wake-word listening ("clippy") so the API is only used while awake. Needs a Vosk model
# folder (see wake.model_path in config.yaml).
vosk>=0.3.44
# The Arduino Bridge client (`arduino.app_utils`, pip package `arduino_app_bricks`) drives the
# MAX7219 matrix via the MCU. It is NOT on PyPI — it comes from the board's App Lab. On the UNO Q,
# make this venv see it (see README "Matriz / Bridge"): drop a `.pth` pointing at the App Lab
# venv's site-packages, or symlink the `arduino` package into this venv. Missing = terminal-only face.
