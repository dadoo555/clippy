# Copie este arquivo para `.env` (na mesma pasta python/) e preencha a chave.
# O `.env` é gitignored — sua chave nunca vai pro repositório.
# Pegue a chave grátis em: https://aistudio.google.com/apikey

GEMINI_API_KEY=coloque-sua-chave-aqui
